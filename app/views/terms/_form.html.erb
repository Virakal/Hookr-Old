<%= form_for @term do |f| %>
  <% if @term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize @term.errors.count, "Error" %> prohibited this term from being saved:</h2>

      <ul>
        <% @term.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :english %><br>
    <%= f.text_field :english %>
  </p>

  <p>
    <%= f.label :american %><br>
    <%= f.text_field :american %>   
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>